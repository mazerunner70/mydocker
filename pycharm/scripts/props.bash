# common properties for all scripts

export pycharm_version_configdir=2019.1
export pycharm_version_download=2019.1.1
export docker_tag=pycharm
#export project_dir="$( cd "$( dirname "${BASH_SOURCE[0]}" )"/.. >/dev/null 2>&1 && pwd )"

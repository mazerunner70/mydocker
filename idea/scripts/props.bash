# common properties for all scripts

export idea_version_configdir=2019.1
export idea_version_download=2019.1.2
export docker_tag=idea
#export project_dir="$( cd "$( dirname "${BASH_SOURCE[0]}" )"/.. >/dev/null 2>&1 && pwd )"
